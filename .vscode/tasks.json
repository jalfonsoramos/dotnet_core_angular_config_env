{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/dotnet_core_angular_config_env.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}